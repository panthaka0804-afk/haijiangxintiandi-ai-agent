# 海江新天地 - 小江AI 后端依赖
# 安装：pip install -r requirements.txt
# Python 3.10+ 验证通过

flask>=3.0.0
openai>=1.30.0          # 代码使用新版 OpenAI() 客户端，接 DeepSeek API
httpx>=0.27.0           # 作为 openai 的 http_client，并用于内部 HTTP 请求
qrcode>=7.4.2           # 生成核销二维码（工单/活动票）
Pillow>=10.0.0          # qrcode 生成图片所需（PIL）
