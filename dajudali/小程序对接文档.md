# 大橘大利AI客服 · 小程序对接文�?
## 接口信息

| 项目 | �?|
|------|-----|
| 接口地址 | `https://tangy-experts-occur.loca.lt/api/ext/chat` |
| 请求方式 | POST |
| 鉴权方式 | Header �?`X-API-Token` |

## Token

```
djdl_8626c6ce03d707dea70e9ef55ca2942e
```

⚠️ 这是测试环境的临�?Token，正式上线会换成正式环境地址�?Token�?
---

## 1. 发送消�?
### 请求

```
POST https://tangy-experts-occur.loca.lt/api/ext/chat
Headers:
  Content-Type: application/json
  X-API-Token: djdl_8626c6ce03d707dea70e9ef55ca2942e

Body (JSON):
{
  "message": "用户在聊天框里打的文�?,
  "user_id": "用户的openid"
}
```

### 返回

```json
{
  "ok": true,
  "reply": "AI客服的回答文�?,
  "tenant_name": "Demo Community"
}
```

### 错误返回

```json
{
  "ok": false,
  "error": "错误原因"
}
```

---

## 2. 清空对话上下�?
用户每次新开对话时调用一次，把之前的对话记录清掉，否则AI会一直记得前面的内容�?
```
POST https://tangy-experts-occur.loca.lt/api/ext/chat/clear
Headers:
  Content-Type: application/json
  X-API-Token: djdl_8626c6ce03d707dea70e9ef55ca2942e

Body (JSON):
{
  "user_id": "用户的openid"
}
```

返回：`{"ok": true, "message": "Context cleared"}`

---

## 3. 获取快捷入口

获取推荐问题的快捷入口列表，可以展示在聊天框上方�?
```
POST https://tangy-experts-occur.loca.lt/api/ext/quick
Headers:
  Content-Type: application/json
  X-API-Token: djdl_8626c6ce03d707dea70e9ef55ca2942e
```

返回�?```json
{
  "ok": true,
  "quick_entries": [
    {"label": "🕐 营业时间", "query": "营业时间"},
    {"label": "🅿�?停车收费", "query": "停车场怎么收费"},
    {"label": "🎉 最新活�?, "query": "最近有什么活�?},
    {"label": "📝 活动报名", "query": "活动报名"},
    {"label": "🔧 我要报修", "query": "我要报修"},
    {"label": "🏪 商务入驻", "query": "怎么入驻开�?}
  ]
}
```

---

## 小程序端调用示例（后端云函数�?
```javascript
// 云函数里调用大橘AI客服
const AI_CHAT_URL = 'https://tangy-experts-occur.loca.lt/api/ext/chat';
const API_TOKEN = 'djdl_8626c6ce03d707dea70e9ef55ca2942e';

async function chatWithAI(userMessage, openid) {
  const res = await fetch(AI_CHAT_URL, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'X-API-Token': API_TOKEN
    },
    body: JSON.stringify({
      message: userMessage,
      user_id: openid
    })
  });
  const data = await res.json();
  if (data.ok) {
    return data.reply;  // AI的回答文�?  } else {
    throw new Error(data.error || 'AI服务异常');
  }
}
```

---

## 注意事项

1. `user_id` 用微信用户的 `openid`，确保每个用户的对话上下文独�?2. 新对话或用完"请空对话"按钮后，调用一�?`/api/ext/chat/clear`
3. 当前是测试环境（localtunnel 隧道），**偶尔需要先在浏览器打开地址点击授权才能正常调用**
4. 正式上线会换成固定域�?+ HTTPS
